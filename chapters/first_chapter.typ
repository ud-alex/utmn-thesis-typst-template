#pagebreak()
  
#import "@preview/theorion:0.6.0": theorem, proof, lemma

= ШАБЛОН ДЛЯ ПОДГОТОВКИ ВЫПУСКНОЙ КВАЛИФИКАЦИОННОЙ РАБОТЫ В СИСТЕМЕ TYPST <core_chapter>

== Общие сведения о шаблоне

В рамках подготовки настоящей выпускной квалификационной работы был разработан шаблон для системы вёрстки Typst, предназначенный для автоматизации оформления текста в соответствии с требованиями Тюменского государственного университета (методические рекомендации кафедры фундаментальной математики и механики). Выбор Typst обусловлен его гибкостью, скоростью компиляции и простой настройки в сравнении с LaTeX.

Шаблон представляет собой набор настроек и пользовательских функций, которые позволяют сосредоточиться на содержании, не отвлекаясь на ручное выравнивание, нумерацию и стили. В основе лежит файл `template.typ`, содержащий все глобальные параметры: поля, шрифт, интервалы, оформление заголовков, списков, таблиц, формул и ссылок. Для демонстрации возможностей шаблона в первую главу вынесено описание его архитектуры и соответствия нормативным документам. Эта глава так же представляет своего рода тест для проверки корректности отображения основных элементов, которые могут встретиться в ВКР. 

== Соответствие требованиям оформления

Методические рекомендации предъявляют ряд формальных требований к внешнему виду работы. Рассмотрим, как эти требования реализованы в шаблоне.

=== Параметры страницы и текста

Согласно разделу 5.1 методички, текст набирается шрифтом Times New Roman размером 14 пт с полуторным межстрочным интервалом. Поля: левое — 30 мм, правое — 15 мм, верхнее и нижнее — по 20 мм.

Красная строка (абзацный отступ) установлена в 1.25 см. Выравнивание текста — по ширине, переносы включены.

Так как Typst несколько иначе трактует некоторые из этих настроек, нежели стандартные офисные пакеты, в настоящей версии на одну страницу вмещается на 2 строки текста больше, чем если набирать текст в LibreOffice (31 против 29; см. последнюю страницу файла). Возможно, исправлю позже.

=== Оформление заголовков и оглавления

Заголовки структурных элементов (ВВЕДЕНИЕ, ГЛАВА, ЗАКЛЮЧЕНИЕ, СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ) должны печататься прописными буквами и располагаться по центру, без переносов.

Нумерация глав выполняется арабскими цифрами с точкой (1., 2., …), параграфы нумеруются двойным номером (1.1., 1.2., …). Для этого настроено `heading(numbering: "1.1.")` для уровней ниже первого. Для заголовков первого уровня нумерация по умолчанию отключена для корректного оформления таких ненумерующихся элементов, как введение, заключение, глоссарий и т.~д. «Содержательные» главы должны сопровождаться тегом `<core_chapter>`. В данном случае слово «ГЛАВА» и нумерация проставляются автоматически. #footnote[Сноски также поддерживаются. Их корректная работа вместе с другими настройками, примененными к тексту, потребовала отдельного тюнинга.]

== Демонстрация дополнительных возможностей

Помимо обязательных элементов, шаблон предоставляет ряд удобных функций, которые существенно упрощают подготовку работы.

=== Нумерация формул

Методические рекомендации допускают как сквозную нумерацию формул, так и нумерацию в пределах главы (например, (1.1), (1.2) и т.д.). В шаблоне реализован переключаемый механизм: по умолчанию используется сквозная нумерация, но если в файле `project_data.yml` установить параметр `numbering_style: nested`, то включается внутриглавная нумерация. При этом номер главы извлекается из счётчика `counter(heading)`, а номер формулы инкрементируется отдельным счётчиком `eq`, который сбрасывается в начале каждой главы. Для формул, на которые не требуется ссылаться, предусмотрена возможность отключения нумерации с помощью пакета `non-unlabeled`, если в настройках указано `numbering_by_default: false`. Это позволяет сохранить аккуратный вид выкладок, не перегружая их лишними номерами.

Формула без нумерации:

$ integral_1^oo 1/x^2 dif x = 1 $

Формула с нумерацией:

$ integral_1^oo 1/x^2 dif x = 1 $ <some_label>


=== Оформление листингов кода и теорем

Для вставки программного кода используется пакет `zebraw`, который обеспечивает подсветку синтаксиса и удобное форматирование. Листинги могут быть пронумерованы и снабжены подписями через механизм `figure`. Например, листинг 1.1 демонстрирует пример использования.

Теоремы, леммы, определения и доказательства оформляются с помощью пакета `theorion`. В шаблоне настроена нумерация вида «1.1.1» (глава.параграф.теорема) через `set-theorion-numbering("1.1.1")`.

#theorem[Теорема-образец][Если реализовывать этот шаблон в LaTeX, то можно быстро сойти с ума.]


=== Оформление таблиц и рисунков

Таблицы, в соответствии с требованиями, должны иметь номер, выровненный по правому краю, и название — по центру. В шаблоне это реализовано через кастомизацию `show figure.where(kind: table)`. Нумерация таблиц и рисунков сквозная, но при желании легко переделать на внутриглавную по аналогии с нумерацией формул. Шаблон по умолчанию не поддерживает автоматической настройки способа нумерации таблиц и рисунков, чтобы чрезмерно не усложнять и удлинять код. В большинстве случаев стандартная сквозная нумерация будет предпочтительна. Рисунки оформляются стандартным образом через `figure` с подписью под изображением. Для вставки графиков и диаграмм достаточно использовать блок `#figure(image(...), caption: "Рис. – Название")`, и шаблон автоматически выставит номер в нужном формате.

Примеры можно посмотреть в @app1 и @app2.


=== Приложения

Для создания приложений в шаблоне предусмотрена отдельная функция `#appendix(appendix_title)`, определённая в файле `appendix.typ`. Она автоматически нумерует приложения (ПРИЛОЖЕНИЕ 1, ПРИЛОЖЕНИЕ 2 …) и размещает заголовок по центру. Каждое приложение начинается с новой страницы, а его внутренние элементы (формулы, таблицы) могут иметь собственную нумерацию с префиксом номера приложения. Ссылки на приложения в основном тексте оформляются стандартно.

== Списки

По умолчанию списки имеют отступ слева величиной в 1,25 см, что равно величине отступа красной строки.

+ Первый пункт списка с нумерацией
+ Второй пункт списка с нумерацией
  + Подпункт второго пункта
+ И так далее

== Список источников

Работа с библиографией по ГОСТу до сих пор остается проблемным местом. Встроенный в Typst стиль поддерживает только нумерацию в порядке появления ссылок в тексте, а алфавитные стили ГОСТ, доступные в Интернете, имеют различные проблемы. 

Самый лучший, который я нашел — это #link("http://www.zotero.org/styles/gost-r-7-0-5-2008-numeric-alphabetical")[gost-r-7-0-5-2008-numeric-alphabetical] (кликабельно). Если проверяющие придирчивы или этот стильно не покрывает всех требований, то можно оформить библиографическое описание вручную и воспользоваться приложенным к шаблону стилем `/assets/manual-entry-numeric.csl`. Он сортирует ссылки сначала по языку (в обратном порядке, то есть `russian` будет выше, чем `english`), потом по имени автора, но выводит только содержимое поля `note`.

Пример: 

```yaml
ivanov2020:
  type: book
  author: Иванов, И.И.
  title: Основы программирования
  year: 2020
  language: english
  note: |
    Иванов И.И. Основы программирования : учебник / И.И. Иванов. – Москва : Наука, 2020. – 300 с. – ISBN 978-5-02-123456-7.
```

Если в тексте поставить ссылку `@ivanov2020`, то в список литературы эта книга войдет точно как `Иванов И.И. Основы программирования : учебник / И.И. Иванов. – Москва : Наука, 2020. – 300 с. – ISBN 978-5-02-123456-7.` 

Можете это проверить @ivanov2020, посмотрев список литературы в конце данного документа, где @anderson2022 также @petrov2021 найдете @rodriguez2023 примеры @sidorov2019 ссылок на другие ресурсы.

По умолчанию используется именно этот способ оформления библиозаписей. Надеюсь, в ближайшее время разработчики Typst сделают такие костыли ненужными.